type Movie {
  id: Int!
  name: String!
  score: Int!
}

type Query {
  movies: [Movie]!
  movie(id: Int!): Movie
}

type Mutation {
    # id는 db에 의해서 자동 생성될거기 때문에 생략해준다
    addMovie(score: Int!, name: String!): Movie!
}